package-as: LootReserve
enable-nolib-creation: no

ignore: # Files and directories beginning with a dot (such as .git) are automatically ignored, as is the pgkmeta file itself.
    - LootReserve.wowproj
    - LICENCE.txt

manual-changelog:
    filename: CHANGELOG.html
    markup-type: html
